# منصة التشريعات اليمنية - متغيرات البيئة
# Yemeni Legislation Platform - Environment Variables

# Database (SQLite)
DATABASE_URL="file:/home/z/my-project/db/custom.db"

# Next.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-in-production"

# GitHub (for CI/CD, not for runtime)
# GH_TOKEN is provided via GitHub Actions secrets, not in .env

# Optional: External integrations
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASS=""

# Platform settings (overridable in admin panel)
# PLATFORM_NAME="منصة التشريعات اليمنية"
